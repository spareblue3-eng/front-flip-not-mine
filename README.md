credits to the original https://scriptblox.com/script/Universal-Script-FrontFlip-Anywhere-30267
you can change the bind by replacing the '8' in these lines: --    UIS.InputBegan:Connect(function(input)
    if input.KeyCode == Enum.KeyCode.8 then -- the keycode is 8
      FortniteFlips()
    end
end)     --
